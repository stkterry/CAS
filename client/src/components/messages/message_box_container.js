import { connect } from "react-redux";
// import { getGames } from "../../actions/game_actions";

import MessageBox from "./MessageBox";

const mSP = state => ({

});


const mDP = dispatch => ({
})

export default connect(mSP, mDP)(MessageBox);